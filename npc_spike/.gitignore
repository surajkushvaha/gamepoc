# Runtime state is generated on first run — keep it out of version control so
# every tester starts from a clean slate.
# data/state.json

# Secrets: real API keys live only in a local .env, never in git.
.env
.env.*
!.env.example

__pycache__/
*.pyc
.venv/
venv/
